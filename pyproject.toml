[tool.poetry]
name = "pxtool"
version = "0.1.0"
description = "Tool for working with pxfi"
authors = ["Team Nettopp <jfi@ssb.no>"]
license = "MIT"

[tool.poetry.dependencies]
python = ">=3.11,<3.12"

[tool.poetry.dev-dependencies]
pytest = "^7.1.3"
ipykernel = "^6.15.3"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
